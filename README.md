# Мои путешествия

Сайт с интерактивным 3D-глобусом: на нём точками отмечены города, куда вы
ездили, каждая точка — это фото. Клик по фото на глобусе, по дню в
календаре справа или по альбому в списке справа открывает окно с
заметкой о поездке и галереей. Клик по фото в галерее открывает его
крупно вместе с подписью, если она есть.

## Структура файлов

- `index.html` — страница сайта
- `style.css` — оформление
- `app.js` — логика глобуса (трогать не нужно)
- `data.js` — **сюда вы вносите свои поездки**
- `photos/` — папка для ваших собственных фото (если не хотите хранить их по ссылкам)

## Как добавить свою поездку

Откройте `data.js` в любом текстовом редакторе (подойдёт Блокнот) и
скопируйте один блок `{ ... }` внутри списка `trips`, затем замените
значения:

```js
{
  city: "Лиссабон",
  country: "Португалия",
  lat: 38.7223,
  lng: -9.1393,
  startDate: "2025-06-10",
  endDate: "2025-06-13",
  dateLabel: "10–13 июня 2025",
  title: "Три дня в Лиссабоне",
  notes: "Трамвай №28, пастель де ната и вид с смотровой площадки.",
  cover: "photos/lisbon/01.jpg",
  photos: [
    { url: "photos/lisbon/01.jpg", caption: "Трамвай №28" },
    { url: "photos/lisbon/02.jpg", caption: "" }
  ]
}
```

Не забудьте поставить запятую `,` после закрывающей `}`, если это не
последний блок в списке.

Координаты (`lat`, `lng`) проще всего найти через Google Maps: кликните
правой кнопкой по нужной точке на карте — вверху появится всплывающее
окно с координатами, нажмите на них, чтобы скопировать.

`startDate`/`endDate` (формат `ГГГГ-ММ-ДД`) — нужны, чтобы дни поездки
подсветились в календаре справа. `dateLabel` — то, что реально
показывается пользователю (можно писать как угодно).

`caption` у фото необязателен — если подписи нет, просто оставьте
пустую строку `""`.

## Свои фотографии

Кладите файлы фото в папку `photos/<название-поездки>/` (например,
`photos/lisbon/01.jpg`). Если фото с телефона весят по несколько
мегабайт — лучше сначала сжать, иначе сайт будет медленно грузиться.
Один файл можно сжать командой (замените пути на свои):

```
ffmpeg -i "исходное-фото.jpg" -vf "scale='min(1600,iw)':-2" -q:v 4 "photos/lisbon/01.jpg"
```

Если фото много — просто пришлите мне папку с оригиналами, я сожму
и разложу их сам.

## Как посмотреть сайт локально

Просто дважды кликните на `index.html` — он откроется в браузере.
Сайт не требует установки программ или сервера.

## Публикация сайта в интернете

Когда будете готовы выложить сайт, чтобы у него был публичный адрес —
скажите, и я помогу настроить бесплатный хостинг на GitHub Pages
шаг за шагом.
